<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".trabajoFragment">

    <!-- El ScrollView debe contener un único ViewGroup como hijo directo -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Este LinearLayout es el único hijo del ScrollView.
             Su orientación vertical asegura que los elementos se apilen. -->
        <LinearLayout
            android:id="@+id/llContenedorFotos"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- El TextView ahora está dentro del LinearLayout -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Imagenes de escuela"
                android:textSize="20sp"
                android:textStyle="bold"
                android:layout_marginBottom="16dp"
                android:layout_gravity="center_horizontal"/>

            <!-- Las ImageView se añadirán aquí dinámicamente -->

        </LinearLayout>

    </ScrollView>
</FrameLayout>
